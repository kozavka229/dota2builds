version: "3.9"

services:
  backend:
    build: .
    container_name: dota2builds-backend
    command: python3 manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - 8000:8000
